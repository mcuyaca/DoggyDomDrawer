---
import { Image } from "astro:assets";

import HeroDog from "../images/doghero.jpeg";
---

<section class="mt-10 flex items-center justify-center gap-10">
  <div class="flex w-[500px] flex-col items-center gap-8">
    <h1 class="text-left text-7xl font-bold text-orange-500">
      Envia tu ubicación de una forma comoda!
    </h1>
    <p class="text-lg">
      Crea un mapa de tu casa para que te encuentre de forma sencilla los
      servicios de delivery.
    </p>
    <button
      class="w-fit border-[3px] border-black bg-white px-20 py-4 text-lg font-bold shadow-[8px_8px_0px_rgba(0,0,0,1)] transition
      duration-300 ease-in-out hover:translate-y-1
     hover:bg-blue-400"
      >Crear mapa 🗺️🐶👍</button
    >
  </div>
  <Image
    class="h-[500px] w-[500px] animate-bounce rounded-3xl border-4 border-black shadow-[8px_8px_0px_rgba(0,0,0,1)] shadow-sm"
    src={HeroDog}
    alt="Un perro mostrando un mapa"
  />
</section>

<style>
  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-20px);
    }
    60% {
      transform: translateY(-5px);
    }
  }
</style>
