---
import { Image } from "astro:assets";

import HeroDog from "../images/doghero.jpeg";
---

<section class="mt-10 flex items-center justify-center gap-10">
  <div class="flex w-[500px] flex-col items-center gap-8">
    <h1 class="text-left text-7xl font-extrabold text-orange-500">
      Envia tu <span class="text-red-600">ubicación</span> de una <span
        class="text-red-600"
      >
        forma comoda!</span
      >
    </h1>
    <p class="text-lg font-medium opacity-80">
      Crea un imagen y añade datos de tu casa para que te encuentre de forma
      sencilla los servicios de delivery.
    </p>
    <button
      class="w-fit animate-bounce border-[3px] border-black bg-orange-400 px-20 py-4 text-lg font-bold shadow-[8px_8px_0px_rgba(0,0,0,1)]
     hover:bg-orange-300"
      ><a href="/draw">Crear mapa 🗺️🐶👍</a></button
    >
  </div>
  <Image
    class="h-[500px] w-[500px] rounded-3xl border-4 border-black shadow-[8px_8px_0px_rgba(0,0,0,1)]"
    src={HeroDog}
    alt="Un perro mostrando un mapa"
  />
</section>

<style>
  @keyframes bounce {
    0%,
    20%,
    50%,
    80%,
    100% {
      transform: translateY(0);
    }
    40% {
      transform: translateY(-5px);
    }
    60% {
      transform: translateY(-5px);
    }
  }
</style>
